PRE REQ CUDA Drivers version 12.4.0 installation

conda create --prefix C:\Users\Raffaele\.conda\envs\simplechat python=3.10 pip
conda activate simplechat
conda install pytorch torchvision torchaudio pytorch-cuda=12.4 -c pytorch -c nvidia

pip install -r requirements.txt

Non usati (non ricordo perché li ho aggiunti)
pip install ninja packaging
pip install flash-attn --no-build-isolation

Esempio di chiamata al server FastAPI con CURL:
curl -X POST http://127.0.0.1:8000/chat/ -H "Content-Type: application/json" -d '{"message": "MESSAGGIO"}'

Comando per generare il server RPC
python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. simple_chat.proto